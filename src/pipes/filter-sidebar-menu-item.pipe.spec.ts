import { FilterSidebarMenuItemPipe } from './filter-sidebar-menu-item.pipe';

describe('FilterSidebarMenuItemPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSidebarMenuItemPipe();
    expect(pipe).toBeTruthy();
  });
});
